@page "/book-viewer/{BookId:int}/{BookTitle?}"

@layout EmptyLayout

@if (string.IsNullOrWhiteSpace(DocumentPath))
{
    <Loading />
}
else
{
    <SfPdfViewer DocumentPath="@DocumentPath" Height="100vh" Width="100vw" />
}
