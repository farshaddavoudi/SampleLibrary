@using ATA.Library.Client.Web.Service.Enums
<div style="direction: ltr; font-family: roboto, iranyekan;height: 70vh; display: flex;justify-content: center;align-items: center;">
    @if (LoadingType == LoadingType.BlueAirplane)
    {
        <img src="/images/components/Loading/airplane-loading.gif" />
    }
    else
    {
        <img src="/images/components/Loading/circle-red-loading.gif" style="width:4rem" />
    }
</div>

@code
{
    [Parameter]
    public LoadingType LoadingType { get; set; } = LoadingType.RedRing;
}
